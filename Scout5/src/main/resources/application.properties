# ================================
# Scout - Fantasy Game
# ================================

spring.application.name=Scout
# Importa variaveis locais do backend e tambem o .env da raiz do projeto.
spring.config.import=optional:file:.env[.properties],optional:file:../.env[.properties]

# ================================
# PostgreSQL - Docker
# docker-compose up -d
spring.datasource.url=jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB}
spring.datasource.username=${POSTGRES_USER}
spring.datasource.password=${POSTGRES_PASSWORD}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true

springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/swagger-ui/index.html
springdoc.swagger-ui.operationsSorter=method

server.port=8080

# ================================
# Log
# ================================
logging.level.com.balbuena=INFO
